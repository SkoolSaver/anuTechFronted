import { industries } from '../helpers/content';
import Card from './Card';

function Industries() {
  return <Card header={'We Serve Multiple Industries'} content={''} cardDetails={industries} />;
}

export default Industries;
